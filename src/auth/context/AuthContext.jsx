import { createContext } from "react";

export const AuthContext = createContext({});

//! CreateContext permite manejar estados globales sin necesidad de pasar props manualmente entre multiples
//! niveles de componentes
